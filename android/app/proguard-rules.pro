# minifyEnabled is off by default for this project (a TWA is a thin native
# shell — there's no app logic here to shrink), so this file is currently
# unused. Kept so build.gradle's proguardFiles reference doesn't break if
# minification is turned on later.
